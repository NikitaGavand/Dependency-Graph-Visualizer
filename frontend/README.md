# GitHub Dependency Graph — Frontend

React SPA with D3.js visualization for code dependency graphs.

## Setup

```bash
npm install
npm start
```

## Build

```bash
npm run build
```

## Environment Variables

- `REACT_APP_API_URL` — Backend API base URL (default: proxy to localhost:5000)
